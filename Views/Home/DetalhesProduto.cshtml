@{

}
@model Pim_da_Web_001.Models.ProdutoViewModel

<h2>Detalhes do Produto</h2>

<div>
    <h4>Produto</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">ID</dt>
        <dd class="col-sm-10">@Model.ID</dd>

        <dt class="col-sm-2">Nome</dt>
        <dd class="col-sm-10">@Model.Nome</dd>

        <dt class="col-sm-2">Valor Venda KG</dt>
        <dd class="col-sm-10">@Model.ValorVendaKG</dd>

        <dt class="col-sm-2">Ativo</dt>
        <dd class="col-sm-10">@Model.Ativo</dd>
    </dl>
</div>
<a href="@Url.Action("Listar", "Produto")" class="btn btn-primary">Voltar</a>
